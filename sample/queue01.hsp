#include "../abdata/queue.as"

	queue_new q
//	mes queue_vartype(q)		// 要素がないので範囲外エラー
	
	repeat 2
		mes "loop "+ cnt
		
		repeat 8
			queue_push q, (1 << cnt)
		loop
		
		repeat queue_size(q)
			mes strf("\t0x%04X", queue_pop(q))
		loop
	loop
	
//	queue_delete q
	stop
	